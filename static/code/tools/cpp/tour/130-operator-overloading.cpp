
// the compiler converts into appropriate function calls
1/a
operator/(complex{1}, a);

// the syntax is fixed by the language so you cannot define a unary /

// it is not possible to change the meaning of an operator for built-in types (e.g. you cannot redefine + to subtract ints)

