#include <iostream>

// every program must have exactly one global function named main() that is executed when the program starts
int main()
{
  std::cout << "Hello, C++!\n";

  // a return value of zero indicates success while a non-zero value indicates failure
  return 0;
}